# General Affairs MCP Server - 사용 가이드

## 📋 개요

General Affairs MCP Server는 e총무 시스템의 기능을 MCP (Model Context Protocol) 툴로 제공하는 서버입니다. AI 에이전트가 자연어로 총무 업무를 처리할 수 있도록 지원합니다.

## 🔧 서버 정보

### 기본 정보

- **서버 이름**: General Affairs MCP Server

- **서버 ID**: `63d753de90e94ef6b0d5d5fc2904051c`

- **Slug**: `general-affairs-mcp-server`

- **Transport**: SSE (Server-Sent Events)

- **상태**: ✅ Active & Reachable

### 연결 정보

- **내부 URL**: `http://mock-up-mcp-server.legacy-mcp.svc.cluster.local:8000/sse`

- **Namespace**: `legacy-mcp`

- **Service**: `mock-up-mcp-server`

- **Port**: 8000

### 백엔드 연결

- **e총무 API**: `http://egeneralaffairs-backend.legacy-general-affairs.svc.cluster.local:8080`

- **인증**: Basic Auth (Admin 계정 사용)

### dify 연결

- **서버 URL**: `https://mcp-gateway.koreacentral.cloudapp.azure.com/sse?server_id=63d753de90e94ef6b0d5d5fc2904051c`

- **이름 및 아이콘**: gw-legacy-general-affairs

- **서버 식별**: gw-legacy-ga

## 🛠️ 제공하는 툴 (총 23개)

### 📋 전체 툴 목록

#### 사용자 관리 (4개)

1. `get_user_info` - 사용자 정보 조회

2. `search_users` - 전체 사용자 목록 조회

3. `search_users_by_name` - 이름으로 사용자 검색

4. `update_user_role` - 사용자 역할 변경

#### 숙박 예약 (3개)

5. `book_accommodation` - 숙박 예약

6. `get_accommodation_status` - 숙박 예약 현황 조회

7. `approve_accommodation` - 숙박 예약 승인

#### 골프 예약 (2개)

8. `reserve_golf` - 골프장 예약

9. `get_golf_status` - 골프 예약 현황 조회

#### 사원증 신청 (2개)

10. `request_idcard` - 사원증 신청

11. `get_idcard_status` - 사원증 신청 현황 조회

#### 로그 관리 (5개)

12. `get_all_login_logs` - 전체 로그인 로그 조회

13. `get_user_login_logs` - 사용자별 로그인 로그 조회

14. `get_all_access_logs` - 전체 액세스 로그 조회

15. `get_user_access_logs` - 사용자별 액세스 로그 조회

16. `get_log_summary` - 로그 요약 정보

#### 분석 & 리포트 (7개)

17. `get_golf_rankings` - 골프 예약 랭킹 (예약 횟수 기준)

18. `get_golf_stats` - 골프 예약 통계

19. `get_accommodation_by_location` - 숙박 위치별 리포트

20. `get_accommodation_stats` - 숙박 예약 통계

21. `get_idcard_stats` - 사원증 신청 통계

22. `get_idcard_by_type` - 사원증 유형별 리포트

23. `get_analytics_dashboard` - 전체 통계 대시보드

---

## 🛠️ 툴 상세 설명

### 1. 사용자 관리 툴

#### `get_user_info`

사용자 정보를 조회합니다.

**파라미터**:

- `user_id` (string): 사용자 ID (예: 'emp006')

**사용 예시**:

```

사용자: emp006 사용자 정보 알려줘

AI: [get_user_info 호출]

```

#### `search_users`

전체 사용자 목록을 조회합니다. (관리자 전용)

**파라미터**: 없음

**사용 예시**:

```

사용자: 전체 사용자 목록 보여줘

AI: [search_users 호출]

```

#### `search_users_by_name`

사용자 이름으로 검색합니다. (관리자 전용)

**파라미터**:

- `username` (string): 검색할 사용자 이름 (부분 일치)

**동작 방식**:

- 전체 사용자 목록(`/api/users/list`)을 조회한 후 클라이언트 측에서 필터링

- `name`, `username`, `userId` 필드에서 부분 일치 검색 (대소문자 무시)

- 백엔드에 `/api/users/search` 엔드포인트가 없어 클라이언트에서 처리

**사용 예시**:

```

사용자: "김" 이 들어간 사용자 찾아줘

AI: [search_users_by_name 호출]

    - username: "김"

사용자: 직원번호 20010001 찾아줘

AI: [search_users_by_name 호출]

    - username: "20010001"

```

#### `update_user_role`

사용자 역할을 변경합니다. (관리자 전용)

**파라미터**:

- `user_id` (string): 사용자 ID

- `role` (string): 새 역할 ('MEMBER', 'ADMIN', 'EXECUTIVE')

**사용 예시**:

```

사용자: emp006을 ADMIN으로 변경해줘

AI: [update_user_role 호출]

```

---

### 2. 숙박 예약 툴

#### `book_accommodation`

숙박 시설을 예약합니다.

**파라미터**:

- `requester_id` (string): 요청자 ID (예: 'emp006')

- `requester_name` (string): 요청자 이름

- `employee_id` (string): 직원 번호 (예: '20240006')

- `phone` (string): 연락처

- `department` (string): 부서

- `check_in` (string): 체크인 날짜 (YYYY-MM-DD)

- `check_out` (string): 체크아웃 날짜 (YYYY-MM-DD)

- `purpose` (string): 목적 ('BUSINESS_TRIP', 'TRAINING' 등)

- `location` (string, optional): 위치 (기본값: 'Seoul')

**사용 예시**:

```

사용자: emp006으로 12월 5일부터 7일까지 서울 출장 숙박 예약해줘

AI: [book_accommodation 호출]

    - requester_id: emp006

    - check_in: 2025-12-05

    - check_out: 2025-12-07

    - purpose: BUSINESS_TRIP

    - location: Seoul

```

#### `get_accommodation_status`

숙박 예약 현황을 조회합니다.

**파라미터**:

- `user_id` (string): 조회할 사용자 ID

**사용 예시**:

```

사용자: 내 숙박 예약 현황 보여줘

AI: [get_accommodation_status 호출]

```

#### `approve_accommodation`

숙박 예약을 승인합니다. (관리자 전용)

**파라미터**:

- `request_id` (int): 승인할 요청 ID

**사용 예시**:

```

사용자: 숙박 예약 123번 승인해줘

AI: [approve_accommodation 호출]

```

---

### 3. 골프장 예약 툴

#### `reserve_golf`

골프장을 예약합니다.

**파라미터**:

- `requester_id` (string): 요청자 ID

- `requester_name` (string): 요청자 이름

- `employee_id` (string): 직원 번호

- `phone` (string): 연락처

- `department` (string): 부서

- `reservation_date` (string): 예약 날짜 (YYYY-MM-DD)

- `number_of_people` (int): 인원 수 (보통 4명)

**사용 예시**:

```

사용자: 12월 10일에 골프장 4명 예약해줘

AI: [reserve_golf 호출]

    - reservation_date: 2025-12-10

    - number_of_people: 4

```

#### `get_golf_status`

골프장 예약 현황을 조회합니다.

**파라미터**:

- `user_id` (string): 조회할 사용자 ID

**사용 예시**:

```

사용자: 내 골프 예약 확인해줘

AI: [get_golf_status 호출]

```

---

### 4. 사원증 신청 툴

#### `request_idcard`

사원증을 신청합니다.

**파라미터**:

- `requester_id` (string): 요청자 ID

- `requester_name` (string): 요청자 이름

- `employee_id` (string): 직원 번호

- `phone` (string): 연락처

- `department` (string): 부서

- `purpose` (string): 신청 사유 ('LOST', 'DAMAGED', 'NEW')

**사용 예시**:

```

사용자: 사원증 분실해서 재발급 신청해줘

AI: [request_idcard 호출]

    - purpose: LOST

```

#### `get_idcard_status`

사원증 신청 현황을 조회합니다.

**파라미터**:

- `user_id` (string): 조회할 사용자 ID

**사용 예시**:

```

사용자: 사원증 신청 상태 확인해줘

AI: [get_idcard_status 호출]

```

---

### 5. 로그 관리 툴

#### `get_all_login_logs`

전체 로그인 로그를 조회합니다. (관리자 전용)

**파라미터**: 없음

**사용 예시**:

```

사용자: 전체 로그인 이력 보여줘

AI: [get_all_login_logs 호출]

```

#### `get_user_login_logs`

특정 사용자의 로그인 로그를 조회합니다.

**파라미터**:

- `user_id` (string): 조회할 사용자 ID

**사용 예시**:

```

사용자: emp006의 로그인 이력 보여줘

AI: [get_user_login_logs 호출]

    - user_id: emp006

```

#### `get_all_access_logs`

전체 액세스 로그를 조회합니다. (관리자 전용)

**파라미터**: 없음

**사용 예시**:

```

사용자: 전체 메뉴 접근 이력 보여줘

AI: [get_all_access_logs 호출]

```

#### `get_user_access_logs`

특정 사용자의 액세스 로그를 조회합니다.

**파라미터**:

- `user_id` (string): 조회할 사용자 ID

**사용 예시**:

```

사용자: emp006이 어떤 메뉴 사용했는지 보여줘

AI: [get_user_access_logs 호출]

    - user_id: emp006

```

#### `get_log_summary`

로그 요약 정보를 조회합니다.

**파라미터**: 없음

**사용 예시**:

```

사용자: 시스템 활용도 요약해줘

AI: [get_log_summary 호출]

```

---

### 6. 분석 & 리포트 툴

#### `get_golf_rankings`

골프 예약 랭킹을 조회합니다. (예약 횟수 기준)

**파라미터**: 없음

**사용 예시**:

```

사용자: 골프 예약 많이 한 사람 누구야?

AI: [get_golf_rankings 호출]

```

#### `get_golf_stats`

골프 예약 통계를 조회합니다.

**파라미터**: 없음

**반환 정보**:

- 전체 예약 수

- 대기 중인 예약

- 승인된 예약

- 총 인원 수

**사용 예시**:

```

사용자: 골프 예약 현황 통계 보여줘

AI: [get_golf_stats 호출]

```

#### `get_accommodation_by_location`

숙박 예약 위치별 리포트를 조회합니다.

**파라미터**: 없음

**반환 정보**:

- 위치별 예약 수

- 위치별 상태(대기/승인/거절)

- 상세 예약 내역

**사용 예시**:

```

사용자: 지역별 출장자 숙소 현황 보여줘

AI: [get_accommodation_by_location 호출]

```

#### `get_accommodation_stats`

숙박 예약 통계를 조회합니다.

**파라미터**: 없음

**반환 정보**:

- 전체 예약 수

- 상태별 분류

- 목적별 분류

- 위치별 분류

**사용 예시**:

```

사용자: 숙박 예약 통계 보여줘

AI: [get_accommodation_stats 호출]

```

#### `get_idcard_stats`

사원증 신청 통계를 조회합니다.

**파라미터**: 없음

**반환 정보**:

- 전체 신청 수

- 상태별 분류(대기/승인/완료)

- 신청 유형별 분류

**사용 예시**:

```

사용자: 사원증 신청 현황 통계 보여줘

AI: [get_idcard_stats 호출]

```

#### `get_idcard_by_type`

사원증 신청 유형별 리포트를 조회합니다.

**파라미터**: 없음

**반환 정보**:

- 유형별 신청 수

- 유형별 상태

- 상세 신청 내역

**사용 예시**:

```

사용자: 사원증 신청 유형별로 정리해줘

AI: [get_idcard_by_type 호출]

```

#### `get_analytics_dashboard`

전체 통계 대시보드를 조회합니다.

**파라미터**: 없음

**반환 정보**:

- 골프 통계

- 숙박 통계

- 사원증 통계

**사용 예시**:

```

사용자: 전체 시스템 현황 대시보드 보여줘

AI: [get_analytics_dashboard 호출]

```

---

## 💬 사용 방법

### 1. MCP Gateway를 통한 사용

MCP Gateway에 이미 등록되어 있으므로, AI 에이전트와 자연어로 대화하면 자동으로 적절한 툴이 호출됩니다.

**대화 흐름**:

```

사용자 (자연어 요청)

    ↓

AI Agent (Claude, GPT 등)

    ↓

MCP Gateway

    ↓

General Affairs MCP Server

    ↓

e총무 백엔드 API

    ↓

응답 반환

```

### 2. 실제 사용 예시

#### 예시 1: 숙박 예약

```

사용자: "emp006으로 다음주 월요일부터 수요일까지 부산 출장 숙박 예약해줘"

AI 처리:

1. 날짜 계산 (다음주 월요일 = 2025-12-08)

2. book_accommodation 툴 호출

3. 결과 반환: "숙박 예약이 완료되었습니다. 요청번호: #456"

```

#### 예시 2: 예약 현황 확인

```

사용자: "내 모든 예약 현황 확인해줘"

AI 처리:

1. get_accommodation_status 호출

2. get_golf_status 호출

3. get_idcard_status 호출

4. 통합 결과 반환

```

#### 예시 3: 복합 작업

```

사용자: "emp006으로 12월 15일 골프 예약하고, 그날 숙박도 예약해줘"

AI 처리:

1. reserve_golf 호출

2. book_accommodation 호출 (같은 날짜)

3. 두 예약 결과 통합 반환

```

### 3. 직접 API 호출 (테스트용)

MCP Gateway를 통하지 않고 직접 테스트할 수도 있습니다:

```bash

# 포트포워딩

kubectl port-forward -n legacy-mcp svc/mock-up-mcp-server 8000:8000

# SSE 엔드포인트 확인

curl http://localhost:8000/sse

```

---

## 🏗️ 기술 스택

### Server

- **Framework**: FastMCP 2.13.2

- **Language**: Python 3.11

- **Transport**: SSE (Server-Sent Events)

- **ASGI Server**: Uvicorn

### Client

- **HTTP Library**: requests

- **Validation**: pydantic

### Infrastructure

- **Container**: Docker

- **Orchestration**: Kubernetes (Azure AKS)

- **Registry**: Azure Container Registry (agenticaidevacr45141.azurecr.io)

- **Image**: `mock-up-mcp-server:v1.7`

---

## 📊 상태 모니터링

### Pod 상태 확인

```bash

kubectl get pods -n legacy-mcp

```

예상 출력:

```

NAME                                 READY   STATUS    RESTARTS   AGE

mock-up-mcp-server-6b4bb7b6f-6r8nt   1/1     Running   0          3h

```

### 로그 확인

```bash

kubectl logs -f deployment/mock-up-mcp-server -n legacy-mcp

```

### MCP Gateway 등록 확인

```bash

# 포트포워딩

kubectl port-forward -n mcp-gateway svc/mcp-gateway 4444:4444

# 등록된 서버 확인

curl http://localhost:4444/gateways | grep "General Affairs"

# 사용 가능한 툴 확인

curl http://localhost:4444/tools

```

---

## 🔐 보안 고려사항

### 인증

- 현재 Admin 계정 (`emp122/1234`)을 사용하여 e총무 API 호출

- 실제 사용자 컨텍스트는 요청 body/params에 포함

### 권한

- 일부 기능은 관리자 전용 (search_users, update_user_role, approve_accommodation)

- 사용자별 데이터는 user_id로 필터링

### 네트워크

- ClusterIP 서비스로 내부 통신만 허용

- 외부 접근은 MCP Gateway를 통해서만 가능

---

## 🔄 업데이트 및 배포

### 이미지 빌드

```bash

cd mock-up-mcp-server

docker build -t agenticaidevacr45141.azurecr.io/mock-up-mcp-server:v1.3 .

```

### ACR 푸시

```bash

az acr login --name agenticaidevacr45141

docker push agenticaidevacr45141.azurecr.io/mock-up-mcp-server:v1.3

```

### Kubernetes 배포 업데이트

```bash

kubectl set image deployment/mock-up-mcp-server \

  mcp-server=agenticaidevacr45141.azurecr.io/mock-up-mcp-server:v1.3 \

  -n legacy-mcp

# 또는 deployment.yaml 수정 후

kubectl apply -f k8s/deployment.yaml

```

---

## 🐛 트러블슈팅

### Pod가 CrashLoopBackOff 상태인 경우

1. 로그 확인: `kubectl logs <pod-name> -n legacy-mcp`

2. FastMCP 버전 확인

3. 환경 변수 확인 (API_BASE_URL)

4. e총무 백엔드 서비스 연결 확인

### MCP Gateway에서 연결 안 되는 경우

1. Service 확인: `kubectl get svc -n legacy-mcp`

2. Reachability 확인: Gateway 로그에서 연결 시도 확인

3. URL 형식 확인: `/sse` 엔드포인트 포함 여부

### 툴 호출 실패하는 경우

1. e총무 백엔드 API 상태 확인

2. 인증 정보 확인 (client.py의 auth)

3. 파라미터 형식 확인

---

## 📞 지원 및 문의

- **Namespace**: `legacy-mcp`

- **Service**: `mock-up-mcp-server`

- **Gateway Registration**: MCP Gateway (`mcp-gateway` namespace)

---

## 📝 변경 이력

### v1.7 (2025-12-03)

- ✅ **로그 관리 기능 추가** (5개 툴)

  - 전체/사용자별 로그인 로그 조회

  - 전체/사용자별 액세스 로그 조회

  - 로그 요약 정보

- ✅ **분석 & 리포트 기능 추가** (7개 툴)

  - 골프 예약 랭킹 및 통계

  - 숙박 위치별 리포트 및 통계

  - 사원증 신청 통계 및 유형별 리포트

  - 전체 통계 대시보드

- ✅ **백엔드 v1.1 배포**

  - LogController 확장 (사용자별 로그 조회)

  - AnalyticsController 신규 생성

- ✅ 서버 ID: `63d753de90e94ef6b0d5d5fc2904051c`

- ✅ **총 23개 MCP 툴 제공** (기존 11개 → 신규 12개 추가)

### v1.6 (2025-12-03)

- ✅ `search_users_by_name` 백엔드 엔드포인트 이슈 수정

- ✅ 클라이언트 측 필터링으로 변경 (백엔드에 `/api/users/search` 없음)

- ✅ 서버 ID: `85b117b023f244278239c96b68ab9534`

### v1.5 (2025-12-03)

- ✅ Pydantic 모델에서 개별 Field 파라미터로 변경

- ✅ 파라미터 검증 오류 수정

### v1.4 (2025-12-03)

- ⚠️ Pydantic BaseModel 사용 시도 (파라미터 검증 오류)

### v1.3 (2025-12-03)

- ✅ `search_users_by_name` 기능 추가

- ✅ 한글 설명 추가

### v1.2 (2025-12-02)

- ✅ FastMCP SSE 모드로 전환

- ✅ server.py에서 직접 포트 제어

- ✅ MCP Gateway 등록 완료

- ✅ 서버 이름 변경: "Mock Up MCP Server" → "General Affairs MCP Server"

### v1.1 (2025-12-02)

- ⚠️ fastmcp dev 명령어 시도 (포트 옵션 미지원으로 실패)

### v1.0 (2025-12-02)

- ⚠️ 초기 배포 (stdio 모드 이슈로 실패)

---

## 🎯 향후 개선 사항

- [ ] 사용자별 인증 구현 (현재는 Admin 계정 공용)

- [ ] 에러 핸들링 강화

- [ ] 로깅 및 모니터링 개선

- [ ] 툴별 사용량 추적

- [ ] 캐싱 메커니즘 추가

- [ ] Health check 엔드포인트 추가

- [ ] 단위 테스트 추가

- [x] ~~사용자 이름 검색 기능 추가~~ (v1.3)

- [x] ~~백엔드 API 엔드포인트 이슈 수정~~ (v1.6)

- [x] ~~로그 관리 및 활용도 평가 기능 추가~~ (v1.7)

- [x] ~~분석 & 리포트 기능 추가~~ (v1.7)